9. Palindrome Number
Given an integer x, return true if x is palindrome integer.

An integer is a palindrome when it reads the same backward as forward. For example, 121 is palindrome while 123 is not.
 题目：
1.检查数字是否回文数字
2.前后相对称
3.如果是有负号，则不成立


 

Example 1:

Input: x = 121
Output: true
Example 2:

Input: x = -121
Output: false
Explanation: From left to right, it reads -121. From right to left, it becomes 121-. Therefore it is not a palindrome.
Example 3:

Input: x = 10
Output: false
Explanation: Reads 01 from right to left. Therefore it is not a palindrome.
Example 4:

Input: x = -101
Output: false
整体思路
1.倒过来写跟原来的数一样
2.前后的数字相同
3.可以用two pointer

class Solution:
    def isPalindrome(self, x: int) -> bool:
        num = 0     先假设置输出值=0
        a = abs(x)   取绝对值，这里先不考虑负号
        
        while(a != 0):    当a 的值不等于0 时
            tem = a% 10     取个位数
            num = num* 10 + tem    将个位数放到十位，一个个往左边移
            a = a// 10    取出个位数后，剩下十位以上的数字，
						                     所以除以10，变回个位，直到剩下0个
            
        
        if x >= 0 and x== num:   当x大于0 时，并且输出值（倒写=原来的数）
            return True    输出真
        else:    小于0时
            return False    不成立
        


