查找表法：哈希表hash 和 平衡二叉搜索法
1.遍历的同事，记录信息，省去一层循环，以空间换时间
2.记录已经遍历的数值和他对应的下标

Python關於刪除list中的某個元素，一般有兩種方法，pop()和remove()。
remove() 函式用於移除列表中某個值的第一個匹配項。
remove()方法語法：
list.remove(obj)
pop() 函式用於移除列表中的一個元素（預設最後一個元素），並且返回該元素的值。
